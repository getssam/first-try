import { MyDerictiveDirective } from './my-derictive.directive';

describe('MyDerictiveDirective', () => {
  it('should create an instance', () => {
    const directive = new MyDerictiveDirective();
    expect(directive).toBeTruthy();
  });
});
